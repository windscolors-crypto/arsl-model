# Shell Voice — نسخة تعمل دون إنترنت (Offline)

هذا المجلد `offline_app` نسخة من التطبيق **بلا أي اعتماد على الإنترنت** — كل المكتبات والنماذج والخطوط صارت محلية، وأُضيف Service Worker يخزّن كل شيء عند أول فتح.

## شو تغيّر عن النسخة المنشورة
- TF.js وMediaPipe Hands وThree.js والخط (Cairo): كانت تُحمّل من CDN خارجي → صارت ملفات محلية داخل `lib/`.
- `text2sign.html` و`avatar.html`: كانت تجيب `sign_animations.json` من رابط github.io → صارت محلية.
- أُضيف: `sw.js` (التخزين للعمل أوفلاين) + `manifest.json` (للتثبيت) + `home.html` (قائمة رئيسية تربط الصفحات الأربع).
- تم التحقق: صفر روابط خارجية في الصفحات الأربع، وكل ملف مطلوب موجود ويُخدَم بنجاح.

## الملفات الرئيسية
- `home.html` ← ابدأي منها (القائمة)
- `index.html` حروف · `words.html` كلمات · `text2sign.html` نص→إشارة · `avatar.html` أفيتار FBX
- `sw.js` · `manifest.json` · مجلد `lib/` (كل المكتبات)

## خطوات النشر على GitHub Pages
ارفعي **كل محتويات** مجلد `offline_app` إلى مستودع `windscolors-crypto/arsl-model` (استبدلي الملفات القديمة بنفس الأسماء، وأضيفي مجلد `lib/` و`sw.js` و`manifest.json` و`home.html`).
- الطريقة الأسهل: على github.com → الريبو → Add file → Upload files → اسحبي كل الملفات والمجلد → Commit.
- بعد دقيقة، الرابط يصير: `https://windscolors-crypto.github.io/arsl-model/home.html`

## اختبار الأوفلاين (المصيري — لازم على جهاز فعلي)
1. افتحي `home.html` على الموبايل **مع إنترنت** مرة وحدة، وانتظري تظهر «✅ جاهز للعمل دون إنترنت».
2. جرّبي الصفحات الأربع مرة (حتى يتخزن كل شي).
3. **سكّري الإنترنت تماماً (وضع الطيران)**، سكّري المتصفح/التطبيق وافتحيه من جديد.
4. لازم كل شي يشتغل: الكاميرا، التعرّف، نص→إشارة — بدون نت.

## لفّها بـ Thunkable
- Web Viewer واحد يفتح `https://windscolors-crypto.github.io/arsl-model/home.html` — وبيغطي كل الصفحات عبر القائمة.
- أو ٤ شاشات/أزرار، كل زر Web Viewer للصفحة المناسبة.
- ⚠️ مهم: لازم نختبر على جهاز فعلي هل Web Viewer تبع Thunkable يحفظ الـ Service Worker بعد إغلاق التطبيق. إذا ما حفظ، الخطة البديلة المضمونة: **تثبيت كـ PWA** (Add to Home Screen) — يعطي أيقونة تطبيق ويشتغل أوفلاين بحكم التصميم.

## ملاحظات
- حجم المجلد ~34MB (طبيعي: نماذج + wasm). يُحمَّل مرة وحدة.
- إذا ما بدك ملفات احتياطية: `lib/mediapipe/hands_solution_wasm_bin.*` و`hand_landmark_*.tflite` احتياط لأجهزة قديمة؛ تركها أأمن.
